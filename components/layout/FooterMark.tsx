/**
 * The footer mark.
 *
 * The old Antonio Marco artwork this rendered has been retired. Its
 * replacement, the AM INDIA full lockup (`brandAssets.fullLockup`), is on
 * disk and ready but has deliberately not been wired in here yet — that is a
 * separate decision, not implied by swapping the header icon. Until that
 * direction is given, the footer renders nothing in this slot rather than a
 * broken image or an unapproved substitution.
 */
export function FooterMark() {
  return null;
}
